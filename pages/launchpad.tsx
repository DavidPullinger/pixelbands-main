// external components
import type { NextPage } from "next";

const Launchpad: NextPage = () => {
  return (
    <div>
      <h1 className="font-bold text-[50px] md:text-[70px] lg:text-[90px]">
        Launchpad
      </h1>
    </div>
  );
};

export default Launchpad;
